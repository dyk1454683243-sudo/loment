// main.lomt —— 这个项目的**宿主**：原生 Loment 语法，负责调模块、决定退出码。
//
// 项目的计算主体在 `algo.lomt` 里，那是**用 C 语法写的 Loment**（`choose write grammar c`）。
// 宿主薄，是因为这一门（以及其余五门）**调不到内建函数、也跨不了单元**：I/O 与 `_start`
// 是宿主的事（见 docs/188 §7.1 的 Stage A 子集）。
module main

use "algo.lomt"

fn _start() {
    // `entry()` 把十三个算法的结果折成一个数 —— 任一算法错了，这个退出码都会变。
    syscall4(60, entry() as u64, 0, 0);
}
